import { Injectable } from '@angular/core';
// import { CookieService as CoreCookieService } from 'ngx-cookie-service';

@Injectable()
export class CookieService {
    // constructor(private cookieService: CoreCookieService) {}

    // getCookie(name) {
    //     return this.cookieService.get(name);
    // }

    // getAllCookie() {
    //     return this.cookieService.getAll();
    // }

    // setCookie(
    //     name: string, value: string, expires: number | Date = new Date(), path: string = '/', domain?: string, secure?: boolean, sameSite?: 'Lax' | 'Strict' | 'None'
    // ) {
    //     this.cookieService.set(name, value, expires, path);
    // }

    // checkCookie(name) {
    //     return this.cookieService.check(name);
    // }

    // deleteCookie(name) {
    //     this.cookieService.delete(name);
    // }

    // deleteAllCookie() {
    //     this.cookieService.deleteAll();
    // }
}